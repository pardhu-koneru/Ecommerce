from django.urls import path
from .views import ReviewViewSet

# Admin-specific URLs for reviews management
# Can be used for internal dashboards, analytics, etc.

urlpatterns = [
    # Custom admin endpoints can be added here if needed
]
